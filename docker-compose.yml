services:
  contact-form:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: contact-form-app
    env_file:
      - .env
    ports:
      - "3001:3000"
    restart: unless-stopped
